#:kivy 2.1.0
#:set font "Nunito"

<ChatBubble>:
    adaptive_height: True
    padding: [dp(8), dp(8)]
    text_color: app.colors["MainColor"]
    text_size: self.width, None
    size_hint_x: 0.8
    halign: self.halign
    font_name: font
    # font_size: dp(app.font_size)
